{
  "tasks": {
    "build": "RUSTFLAGS=--cfg=web_sys_unstable_apis deno run -A --unstable https://raw.githubusercontent.com/denoland/wasmbuild/main/main.ts",
    "release": "deno task build --release && wasm-opt -Oz lib/eszip_wasm_bg.wasm -o lib/eszip_wasm_bg.wasm",
    "node": "deno run -A ./build_npm.ts 0.0.0",
    "test": "deno test -A lib/",
    "fmt": "deno fmt lib/ build_npm.ts && cargo fmt --all"
  }
}
