{
  "fmt": {
    "files": {
      "exclude": [
        "./npm",
        "./src/testdata/",
        "./target"
      ]
    }
  },
  "tasks": {
    "build": "RUSTFLAGS=--cfg=web_sys_unstable_apis deno run -A --unstable --no-check https://deno.land/x/wasmbuild@0.7.0/main.ts",
    "node": "deno run -A ./build_npm.ts 0.0.0",
    "test": "deno test -A lib/",
    "fmt": "deno fmt && cargo fmt --all"
  }
}
